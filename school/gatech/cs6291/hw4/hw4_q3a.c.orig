#include <stdio.h>
#include <stdlib.h>

int main(void)
{
    int vals[] = {10, 7};
    int b = vals[0];
    int c = vals[1];
    
    int a = b + c;
    int d = a;
    int e = a + 100;
    a = e - 10;
    d += 10;

    printf("a=%d d=%d e=%d\n", a, d, e);


    return EXIT_SUCCESS;
}

